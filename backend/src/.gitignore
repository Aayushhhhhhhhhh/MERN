# ---------------------------------------------------------------------
# Environments & Secrets (NEVER commit api keys, passwords, or tokens)
# ---------------------------------------------------------------------
.env
.env.local
*.pem
credentials.json

# ---------------------------------------------------------------------
# Dependencies (Downloadable packages should not clutter source code)
# ---------------------------------------------------------------------
node_modules/
/venv/
.venv/
/env/
vendor/

# ---------------------------------------------------------------------
# Build & Output Directories (Compiled code or bundling results)
# ---------------------------------------------------------------------
dist/
build/
out/
*.class
*.pyc

# ---------------------------------------------------------------------
# Logs and Caches (Temporary operational files)
# ---------------------------------------------------------------------
*.log
npm-debug.log*
.pytest_cache/
.eslintcache

# ---------------------------------------------------------------------
# OS & IDE Junk (System or personal editor preferences)
# ---------------------------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
